# Building an App with the TS SDK

*(Placeholder — content pending.)*

Scope: using the [SDK TypeScript](../sdk-ts/index.md) client inside an app session — what's available given the manifest's declared permissions, using `apps/examples/dashboard` as the fuller reference.
