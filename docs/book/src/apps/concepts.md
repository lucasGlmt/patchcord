# Concepts

*(Placeholder — content pending.)*

Scope: session-limited hosting model (ADR-0026, `internal/apps/apps.go`) — an app session is restricted to the permissions declared in its manifest, not full agent access.
