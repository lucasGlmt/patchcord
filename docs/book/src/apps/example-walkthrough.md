# Example Walkthrough

*(Placeholder — content pending.)*

Scope: end-to-end walkthrough of `apps/examples/greeter` — install it, run `patchcord serve`, open `/apps/greeter/`, trace the single `hello_patchcord` workflow permission through to the running page.
