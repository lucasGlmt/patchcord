# Hosting & Sessions

*(Placeholder — content pending.)*

Scope: how `patchcord app install <dir>` serves a static app under `/apps/<id>/`, and how a session's permissions are enforced against the manifest at runtime.
