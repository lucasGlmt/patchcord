# Apps Overview

*(Placeholder — content pending.)*

Scope: what an application is per the [vocabulary](../introduction.md#vocabulary) — a client of the agent using only the public API and the TypeScript SDK, never running with full admin privileges. Map of the two examples: `apps/examples/greeter` (minimal reference) and `apps/examples/dashboard` (generic workflow exerciser).
