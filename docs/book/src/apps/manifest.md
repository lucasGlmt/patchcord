# App Manifest

*(Placeholder — content pending.)*

Scope: `patchcord-app.yaml` field-by-field reference (`internal/apps/manifest.go`) — `id`, `version`, `permissions.workflows.run`, using `apps/examples/greeter/patchcord-app.yaml` as the annotated example.
