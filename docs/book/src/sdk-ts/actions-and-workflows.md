# Actions & Workflows

*(Placeholder — content pending.)*

Scope: triggering a workflow run and inspecting its result via the SDK (`src/run.ts`), mirroring [Workflows → Triggering](../workflows/triggering.md) from the client side.
