# Core Concepts

*(Placeholder — content pending.)*

Scope: how SDK types (`src/types.ts`, `src/wire.ts`) map to the vocabulary in the [Introduction](../introduction.md#vocabulary) — workflow, run, connector, app — and to the public API contracts under `api/`.
