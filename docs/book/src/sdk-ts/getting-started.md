# Getting Started

*(Placeholder — content pending.)*

Scope: installing the package, instantiating the client (`src/client.ts`), pointing it at a running `patchcord serve` instance, and a minimal "hello world" call.
