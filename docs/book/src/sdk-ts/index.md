# SDK TypeScript Overview

*(Placeholder — content pending.)*

Scope: what the SDK is (`sdk/typescript`), the split between `client.ts`, `run.ts`, `sse.ts`, `wire.ts`, and `types.ts`, and its role as *the* way applications talk to the agent — no parallel logic outside the public API.
