# Types & Contracts

*(Placeholder — content pending.)*

Scope: how SDK types stay in sync with the public API contracts (`api/agent` OpenAPI spec, ADR-0025) and what happens across a contract version bump.
