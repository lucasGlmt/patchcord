# Events

*(Placeholder — content pending.)*

Scope: real-time run events over SSE, implemented via polling (ADR-0019) rather than a push-based mechanism — what that means for consumers (polling interval, ordering guarantees).
