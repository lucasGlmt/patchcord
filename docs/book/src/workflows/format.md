# Workflow Format

*(Placeholder — content pending.)*

Scope: the YAML/JSON workflow definition structure (`internal/workflow/definition.go`), how it compiles (`compile.go`), and the expression language for bindings (`expr.go`). Include a minimal annotated example workflow.
