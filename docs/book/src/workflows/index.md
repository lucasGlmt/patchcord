# Workflows Overview

*(Placeholder — content pending.)*

Scope: what a workflow is (declarative orchestration of actions), where the engine lives (`internal/workflow/`), and a map of the sub-pages: concepts, format, runs, triggering, events, timeouts.
