# Runs

*(Placeholder — content pending.)*

Scope: the run state machine (`internal/workflow/state.go`) — valid and invalid transitions between `queued`, `running`, `succeeded`, `failed`, `cancelled`, backed by the same table-driven tests that guard the engine.
