# Timeouts & Cancellation

*(Placeholder — content pending.)*

Scope: how a step timeout is enforced, and what happens to remaining steps when a run is cancelled or a step times out (ADR-0018) — `context.Context` propagation through the engine.
