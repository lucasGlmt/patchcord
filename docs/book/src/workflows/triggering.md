# Triggering Workflows

*(Placeholder — content pending.)*

Scope: the two ways to start a run — `patchcord run` (CLI) and the asynchronous HTTP trigger endpoint (ADR-0024) — and how they both go through the same internal service (ADR-0005).
